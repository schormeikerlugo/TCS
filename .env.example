# Resend API key — get it at https://resend.com/api-keys
# Local: copy this file to `.env` and paste your key.
# Production: set RESEND_API_KEY in Vercel → Project → Settings → Environment Variables.
RESEND_API_KEY=
